confluence:
  image: cptactionhank/atlassian-confluence:latest
#  restart: always
#  cpu_shares: 100
#  mem_limit: 209715200
  ports:
    - "80:8090"
  links:
    - postgres

postgres:
  image: postgres
#  cpu_shares: 100
#  mem_limit: 209715200
  ports:
    - "5432:5432"
  volumes:
    - ./scripts:/docker-entrypoint-initdb.d/
